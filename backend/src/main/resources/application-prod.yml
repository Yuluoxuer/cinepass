# ============ 生产环境配置 ============

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:8.134.24.207}:${DB_PORT:5432}/${DB_NAME:miaoyu}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Rhongomyniad0328}
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      validation-query: SELECT 1
      test-while-idle: true

  redis:
    host: ${REDIS_HOST:8.134.24.207}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: 0

  elasticsearch:
    uris: http://${ES_HOST:8.166.137.17}:${ES_PORT:9200}

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  refresh-expire-seconds: ${JWT_REFRESH_EXPIRE_SECONDS:604800}


logging:
  level:
    com.cinepass: debug
    org.springframework: info
