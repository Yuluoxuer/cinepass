<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinepass.mapper.SeatLockMapper">

    <resultMap id="LockMap" type="com.cinepass.model.SeatLock">
        <id property="lockId" column="lock_id"/>
        <result property="showId" column="show_id"/>
        <result property="userId" column="user_id"/>
        <result property="seatIdsJson" column="seat_ids_json"/>
        <result property="status" column="status"/>
        <result property="ttlSeconds" column="ttl_seconds"/>
        <result property="expireAt" column="expire_at"/>
        <result property="sessionId" column="session_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- PostgreSQL: seat_ids_json 为 jsonb，须 CAST -->
    <insert id="insert" databaseId="postgresql">
        INSERT INTO seat_lock (
            lock_id, show_id, user_id, seat_ids_json, status, ttl_seconds,
            expire_at, session_id, created_at, updated_at
        ) VALUES (
            #{lockId}, #{showId}, #{userId}, CAST(#{seatIdsJson} AS jsonb), #{status}, #{ttlSeconds},
            #{expireAt}, #{sessionId}, #{createdAt}, #{updatedAt}
        )
    </insert>
    <insert id="insert">
        INSERT INTO seat_lock (
            lock_id, show_id, user_id, seat_ids_json, status, ttl_seconds,
            expire_at, session_id, created_at, updated_at
        ) VALUES (
            #{lockId}, #{showId}, #{userId}, #{seatIdsJson}, #{status}, #{ttlSeconds},
            #{expireAt}, #{sessionId}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findByIdForUpdate" databaseId="postgresql" resultMap="LockMap">
        SELECT * FROM seat_lock WHERE lock_id = #{lockId} FOR UPDATE
    </select>
    <select id="findByIdForUpdate" resultMap="LockMap">
        SELECT * FROM seat_lock WHERE lock_id = #{lockId}
    </select>

    <select id="findById" resultMap="LockMap">
        SELECT * FROM seat_lock WHERE lock_id = #{lockId}
    </select>

    <update id="markReleased" databaseId="postgresql">
        UPDATE seat_lock
        SET status = 'released', updated_at = NOW()
        WHERE lock_id = #{lockId} AND status = 'active'
    </update>
    <update id="markReleased">
        UPDATE seat_lock
        SET status = 'released', updated_at = CURRENT_TIMESTAMP
        WHERE lock_id = #{lockId} AND status = 'active'
    </update>

    <update id="markExpired" databaseId="postgresql">
        UPDATE seat_lock
        SET status = 'expired', updated_at = NOW()
        WHERE lock_id = #{lockId} AND status = 'active'
    </update>
    <update id="markExpired">
        UPDATE seat_lock
        SET status = 'expired', updated_at = CURRENT_TIMESTAMP
        WHERE lock_id = #{lockId} AND status = 'active'
    </update>

    <update id="markConsumed" databaseId="postgresql">
        UPDATE seat_lock
        SET status = 'consumed', updated_at = NOW()
        WHERE lock_id = #{lockId} AND status = 'active'
    </update>
    <update id="markConsumed">
        UPDATE seat_lock
        SET status = 'consumed', updated_at = CURRENT_TIMESTAMP
        WHERE lock_id = #{lockId} AND status = 'active'
    </update>
</mapper>
