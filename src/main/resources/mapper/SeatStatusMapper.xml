<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinepass.mapper.SeatStatusMapper">

    <resultMap id="StatusMap" type="com.cinepass.model.SeatStatus">
        <id property="showId" column="show_id"/>
        <id property="seatId" column="seat_id"/>
        <result property="status" column="status"/>
        <result property="lockId" column="lock_id"/>
        <result property="userId" column="user_id"/>
        <result property="expireAt" column="expire_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="batchInsert" databaseId="postgresql">
        INSERT INTO seat_status (show_id, seat_id, status, updated_at) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.showId}, #{item.seatId}, #{item.status}, NOW())
        </foreach>
        ON CONFLICT (show_id, seat_id) DO NOTHING
    </insert>
    <insert id="batchInsert">
        INSERT INTO seat_status (show_id, seat_id, status, updated_at) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.showId}, #{item.seatId}, #{item.status}, CURRENT_TIMESTAMP)
        </foreach>
    </insert>

    <select id="countAvailable" databaseId="postgresql" resultType="int">
        SELECT COUNT(1) FROM seat_status
        WHERE show_id = #{showId} AND status = 'available'
    </select>
    <select id="countAvailable" resultType="int">
        SELECT COUNT(1) FROM seat_status
        WHERE show_id = #{showId} AND status = 'available'
    </select>

    <select id="countTotal" resultType="int">
        SELECT COUNT(1) FROM seat_status WHERE show_id = #{showId}
    </select>

    <select id="selectByShowId" resultMap="StatusMap">
        SELECT show_id, seat_id, status, lock_id, user_id, expire_at, updated_at
        FROM seat_status
        WHERE show_id = #{showId}
    </select>

    <select id="selectForUpdate" databaseId="postgresql" resultMap="StatusMap">
        SELECT show_id, seat_id, status, lock_id, user_id, expire_at, updated_at
        FROM seat_status
        WHERE show_id = #{showId}
          AND seat_id IN
        <foreach collection="seatIds" item="id" open="(" separator="," close=")">#{id}</foreach>
        ORDER BY seat_id
        FOR UPDATE
    </select>
    <select id="selectForUpdate" resultMap="StatusMap">
        SELECT show_id, seat_id, status, lock_id, user_id, expire_at, updated_at
        FROM seat_status
        WHERE show_id = #{showId}
          AND seat_id IN
        <foreach collection="seatIds" item="id" open="(" separator="," close=")">#{id}</foreach>
        ORDER BY seat_id
    </select>

    <update id="releaseExpired" databaseId="postgresql">
        UPDATE seat_status
        SET status = 'available',
            lock_id = NULL,
            user_id = NULL,
            expire_at = NULL,
            updated_at = NOW()
        WHERE show_id = #{showId}
          AND seat_id IN
        <foreach collection="seatIds" item="id" open="(" separator="," close=")">#{id}</foreach>
          AND status = 'locked'
          AND expire_at IS NOT NULL
          AND expire_at &lt;= #{now}
    </update>
    <update id="releaseExpired">
        UPDATE seat_status
        SET status = 'available',
            lock_id = NULL,
            user_id = NULL,
            expire_at = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE show_id = #{showId}
          AND seat_id IN
        <foreach collection="seatIds" item="id" open="(" separator="," close=")">#{id}</foreach>
          AND status = 'locked'
          AND expire_at IS NOT NULL
          AND expire_at &lt;= #{now}
    </update>

    <update id="releaseExpiredByShow" databaseId="postgresql">
        UPDATE seat_status
        SET status = 'available',
            lock_id = NULL,
            user_id = NULL,
            expire_at = NULL,
            updated_at = NOW()
        WHERE show_id = #{showId}
          AND status = 'locked'
          AND expire_at IS NOT NULL
          AND expire_at &lt;= #{now}
    </update>
    <update id="releaseExpiredByShow">
        UPDATE seat_status
        SET status = 'available',
            lock_id = NULL,
            user_id = NULL,
            expire_at = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE show_id = #{showId}
          AND status = 'locked'
          AND expire_at IS NOT NULL
          AND expire_at &lt;= #{now}
    </update>

    <update id="markLocked" databaseId="postgresql">
        UPDATE seat_status
        SET status = 'locked',
            lock_id = #{lockId},
            user_id = #{userId},
            expire_at = #{expireAt},
            updated_at = NOW()
        WHERE show_id = #{showId}
          AND seat_id IN
        <foreach collection="seatIds" item="id" open="(" separator="," close=")">#{id}</foreach>
          AND status = 'available'
    </update>
    <update id="markLocked">
        UPDATE seat_status
        SET status = 'locked',
            lock_id = #{lockId},
            user_id = #{userId},
            expire_at = #{expireAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE show_id = #{showId}
          AND seat_id IN
        <foreach collection="seatIds" item="id" open="(" separator="," close=")">#{id}</foreach>
          AND status = 'available'
    </update>

    <!-- 取消订单/解锁时按 lockId 释放仍锁定的座位 -->
    <update id="releaseByLockId" databaseId="postgresql">
        UPDATE seat_status
        SET status = 'available',
            lock_id = NULL,
            user_id = NULL,
            expire_at = NULL,
            updated_at = NOW()
        WHERE lock_id = #{lockId}
          AND status = 'locked'
    </update>
    <update id="releaseByLockId">
        UPDATE seat_status
        SET status = 'available',
            lock_id = NULL,
            user_id = NULL,
            expire_at = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE lock_id = #{lockId}
          AND status = 'locked'
    </update>
</mapper>
